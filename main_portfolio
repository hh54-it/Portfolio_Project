<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hussein Harb - Portfolio</title>
    <style>
        /* Reset CSS */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f7f7f7;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        header {
            background-color: #1a1a1a;
            color: #fff;
            padding: 20px 0;
            text-align: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        h1 {
            font-size: 2.5rem;
            font-weight: 600;
        }

        nav {
            margin-top: 15px;
        }

        nav ul {
            list-style-type: none;
            display: flex;
            justify-content: center;
        }

        nav ul li {
            margin: 0 15px;
            font-size: 1rem;
            transition: color 0.3s ease;
        }

        nav ul li a {
            color: #fff;
            text-transform: uppercase;
            font-weight: 500;
        }

        nav ul li:hover a {
            color: #00aaff;
        }

        .hamburger {
            display: none;
            cursor: pointer;
            font-size: 2rem;
            color: #fff;
        }

        /* Main Content */
        main {
            padding: 50px 15%;
        }

        section {
            margin-bottom: 50px;
        }

        section h2 {
            font-size: 2rem;
            margin-bottom: 15px;
            color: #333;
            font-weight: 500;
        }

        section p {
            font-size: 1.1rem;
            color: #555;
        }

        /* Projects Section */
        .project {
            margin-bottom: 20px;
        }

        .project h3 {
            font-size: 1.5rem;
            color: #333;
        }

        .project p {
            font-size: 1rem;
            color: #777;
            margin-bottom: 15px;
        }

        .project a {
            color: #00aaff;
            transition: color 0.3s ease;
        }

        .project a:hover {
            color: #0099cc;
        }

        /* Carousel Styling */
        .carousel {
            position: relative;
            max-width: 600px;
            margin: 30px auto;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .carousel-images img {
            width: 100%;
            display: none;
            transition: opacity 1s ease;
        }

        .carousel-images img.active {
            display: block;
            opacity: 1;
        }

        .carousel-images img.inactive {
            opacity: 0;
        }

        .prev, .next {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            background-color: rgba(0, 0, 0, 0.5);
            color: #fff;
            border: none;
            padding: 10px;
            cursor: pointer;
            border-radius: 50%;
            transition: background-color 0.3s ease;
        }

        .prev:hover, .next:hover {
            background-color: rgba(0, 0, 0, 0.8);
        }

        .prev {
            left: 10px;
        }

        .next {
            right: 10px;
        }

        /* Contact Section */
        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        form input, form textarea {
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        form button {
            background-color: #00aaff;
            color: #fff;
            border: none;
            padding: 10px;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        form button:hover {
            background-color: #0099cc;
        }

        footer {
            text-align: center;
            padding: 20px;
            background-color: #1a1a1a;
            color: #fff;
        }

        /* Responsive Design */
        @media screen and (max-width: 768px) {
            nav ul {
                display: none;
                flex-direction: column;
                align-items: center;
                margin-top: 20px;
            }

            nav ul.show {
                display: flex;
            }

            nav ul li {
                margin: 10px 0;
            }

            header {
                padding: 20px;
            }

            .hamburger {
                display: block;
                position: absolute;
                top: 20px;
                right: 20px;
            }

            main {
                padding: 30px 10%;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Welcome to My Portfolio</h1>
        <div class="hamburger" onclick="toggleMenu()">&#9776;</div>
        <nav>
            <ul id="nav-menu">
                <li><a href="#about-me">About Me</a></li>
                <li><a href="#projects">Projects</a></li>
                <li><a href="#skills">Skills</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
    </header>

    <main>
       
        <section id="about-me">
            <h2>About Me</h2>
            <p>My name is Hussein Harb, an undergraduate student at NJIT. Here are some projects and skills that represent my capabilities.</p>
        </section>

        
        <section id="projects">
            <h2>My Projects</h2>
            <div class="project">
                <h3>Project 1: Store Website</h3>
                <p>This is a website for store managers to manage inventory and view sales reports. I used HTML, JavaScript, PHP, CSS, and SQL to build it.</p>
                <a href="https://github.com/yourusername/project1" target="_blank">View on GitHub</a>
            </div>

            
            <div class="carousel">
                <div class="carousel-images">
                    <img src="project1.jpg" alt="Project 1" class="active">
                    <img src="project2.jpg" alt="Project 2">
                    <img src="project3.jpg" alt="Project 3">
                </div>
                <button class="prev" onclick="moveSlide(-1)">&#10094;</button>
                <button class="next" onclick="moveSlide(1)">&#10095;</button>
            </div>
        </section>

        <section id="skills">
            <h2>My Skills</h2>
            <ul>
                <li>Python, C++, Java, HTML</li>
                <li>Git, GitHub</li>
            </ul>
        </section>

       
        <section id="contact">
            <h2>Contact</h2>
            <form id="contact-form">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" required>

                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required>

                <label for="message">Message:</label>
                <textarea id="message" name="message" required></textarea>

                <button type="submit">Send Message</button>
            </form>
        </section>
    </main>

    <footer>
        <p>&copy; 2025 Hussein Harb. All rights reserved.</p>
    </footer>

    <script>
        
        function toggleMenu() {
            document.getElementById('nav-menu').classList.toggle('show');
        }

        
        let index = 0;

        function moveSlide(step) {
            const slides = document.querySelectorAll('.carousel-images img');
            index += step;

            if (index < 0) {
                index = slides.length - 1;
            } else if (index >= slides.length) {
                index = 0;
            }

            for (let i = 0; i < slides.length; i++) {
                slides[i].classList.remove('active');
                slides[i].classList.add('inactive');
            }

            slides[index].classList.add('active');
            slides[index].classList.remove('inactive');
        }

        
        document.getElementById('contact-form').addEventListener('submit', function (event) {
            const name = document.getElementById('name').value;
            const email = document.getElementById('email').value;
            const message = document.getElementById('message').value;

            if (!name || !email || !message) {
                alert('Please fill in all fields.');
                event.preventDefault();
            }
        });
    </script>
</body>

</html>
